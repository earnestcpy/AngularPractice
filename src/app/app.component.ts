import { Component } from '@angular/core';

@Component({
  selector: 'app-root', /*  Create a new component or tag named "app-root" */
  templateUrl: './app.component.html', /* HTML  template for app-root */
  styleUrls: ['./app.component.css'] /* Style // CSS for app-root */
})
export class AppComponent {
  title = 'todo';
}
